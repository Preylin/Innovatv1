name: CI/CD Rootless Docker

on:
  push:
    branches: [ main ]  # Dispara solo al push en main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      REGISTRY: ghcr.io/preylin   # Cambia a tu Docker Hub si quieres
      PROJECT: innovatv1          # Nombre del proyecto
      VPS_USER: deploy
      VPS_IP: 66.94.108.125
      SSH_KEY: ${{ secrets.VPS_SSH_KEY }}  # Clave SSH privada del deploy user

    steps:
      # 1️⃣ Checkout del repo
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Login al registry (GHCR o Docker Hub)
      - name: Login to registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_TOKEN }}

      # 3️⃣ Build imagen FastAPI
      - name: Build FastAPI image
        run: |
          docker build -t $REGISTRY/$innovatv1:fastapi ./api

      # 4️⃣ Build imagen frontend
      - name: Build Frontend image
        run: |
          docker build -t $REGISTRY/$innovatv1:frontend ./frontend

      # 5️⃣ Push imágenes al registry
      - name: Push images
        run: |
          docker push $REGISTRY/$innovatv1:fastapi
          docker push $REGISTRY/$innovatv1:frontend

      # 6️⃣ Deploy al VPS
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ env.VPS_IP }}
          username: ${{ env.VPS_USER }}
          key: ${{ env.SSH_KEY }}
          script: |
            cd ~/docker/innovatv1
            # Actualizar imágenes desde el registry
            docker pull $REGISTRY/$innovatv1:fastapi
            docker pull $REGISTRY/$innovatv1:frontend
            # Levantar containers rootless
            docker compose up -d
