server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Esto permite que TanStack Router maneje las rutas
        try_files $uri $uri/ /index.html;
    }
}